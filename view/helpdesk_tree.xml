<?xml version="1.0"?>
<!-- This file is part of the helpdesk module for Tryton.
The COPYRIGHT file at the top level of this repository contains the full
copyright notices and license terms. -->
<tree string="Helpdesk" colors="If(In(Eval('state', ''), ['cancel', 'done']), 'grey', If(Equal(Eval('state', ''), 'open'), 'red', 'black'))">
    <field name="date"/>
    <field name="party"/>
    <field name="contact" tree_invisible="1"/>
    <field name="email_from"/>
    <field name="email_cc" tree_invisible="1"/>
    <field name="smtp_server" tree_invisible="1"/>
    <field name="name"/>
    <field name="employee"/>
    <field name="priority"/>
    <field name="state"/>
    <field name="last_talk"/>
    <field name="num_attach"/>
</tree>
